interface:
  display_name: "OSS Thanks"
  short_description: "Review or star open-source projects used by agents."
  default_prompt: "Use $oss-thanks to record GitHub repositories referenced during this task and review or star them according to the configured consent mode."
policy:
  allow_implicit_invocation: true
